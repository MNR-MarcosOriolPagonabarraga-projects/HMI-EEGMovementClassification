\begin{table*}[!b]
  \centering
  \begingroup
  \captionsetup{font=normalsize}
  \caption{Held-out four-class classification accuracy on the withheld validation fraction.}
  \label{tab:main}
  \normalsize
  \setlength{\tabcolsep}{14pt}
  \renewcommand{\arraystretch}{1.55}
  \begin{tabularx}{\textwidth}{@{} >{\raggedright\arraybackslash}p{3.05cm} >{\raggedright\arraybackslash}X >{\centering\arraybackslash}p{2.05cm} @{}}
    \toprule
    \textbf{Approach series} & \textbf{Representations \& notes} & \textbf{Accuracy [\%]} \\
    \midrule
    Block~I CSP+LDA~\cite{gramfort2013meg} &
      Motor-band CSP after search across band hypotheses. &
      \(\sim\)36 \\
    Block~II CNN baseline~\cite{lawhern2018eegnet} &
      Raw EEG via separable temporal convolutions~(EEGNet-class baseline). &
      \(\sim\)56 \\
    Block~II CNN + PSD~(EEGPSDNet) &
      Late fusion: raw epochs and Welch PSD maps. &
      \(\sim\)61 \\
    Block~II CNN + CSD attn.~(ConnNet) &
      Raw epochs and coherence tensors with modality attention. &
      \(\sim\)61\,--\,62 \\
    \bottomrule
  \end{tabularx}
  \endgroup
\end{table*}

\section{Experimental results}
\label{sec:results}

\subsection{Stratification and accuracy benchmarks}

\label{sec:accuracy}
Models were judged on withheld validation accuracy from the stratified pooled-subject split described in Section~\ref{sec:dataprep}.
CNN checkpoints correspond to maximal validation accuracy during optimisation.
CSP+LDA occupies the pooled training partition for estimation and withheld validation for unbiased accuracy.



\subsection{Connectivity scalp contrast maps}

\label{sec:topo}

Figure~\ref{fig:connTopo} summarizes trial-averaged connectivity-derived differences between pooled hand synergy epochs and rest on the standard Graz electrode layout~\cite{ofner2017upper}.
Placement uses a single-column raster width with a tight ``here'' float anchor so it typically appears immediately beneath this subsection rather than drifting to later pages.

\begin{figure}[H]
  \centering
  \includegraphics[width=\linewidth]{connectivity_topomaps}
  \caption{Trial-averaged connectivity-derived spatial patterns contrasting hand synergy epochs versus rest~(Graz layout~\cite{ofner2017upper}).}
  \label{fig:connTopo}
\end{figure}

\subsection{\texorpdfstring{$t$-Distributed stochastic neighbour embedding of softmax precursors}{t-Distributed stochastic neighbour embedding of softmax precursors}}
\label{sec:tsne}

Figure~\ref{fig:tsne} occupies the full text width~(two-column span) so the three Block~II embedding panels remain legible; star-floats normally appear at the \emph{top} or \emph{bottom} of a page when sufficient space opens.
Embeddings apply identical $t$-SNE hyper-parameters to withheld-trial softmax input vectors across architectures.

\begin{figure*}[!tb]
  \centering
  \captionsetup[subfigure]{skip=6pt,width=0.9\linewidth,font=small}
  \begin{subfigure}[t]{0.32\textwidth}
    \centering
    \includegraphics[width=\linewidth]{TSNE_EEGNet_Latent_Space_predecision}
    \caption{CNN baseline.}
    \label{fig:tsne-eegnet}
  \end{subfigure}\hfill
  \begin{subfigure}[t]{0.32\textwidth}
    \centering
    \includegraphics[width=\linewidth]{TSNE_EEGPsdNet_Latent_Space_predecision}
    \caption{CNN + PSD fusion.}
    \label{fig:tsne-psd}
  \end{subfigure}\hfill
  \begin{subfigure}[t]{0.32\textwidth}
    \centering
    \includegraphics[width=\linewidth]{TSNE_EEGConnNet_Latent_Space_predecision}
    \caption{CNN + connectivity fusion.}
    \label{fig:tsne-conn}
  \end{subfigure}\\[6pt]
  \caption{\(t\)-SNE embeddings of withheld-trial softmax input vectors.}
  \label{fig:tsne}
\end{figure*}

Geometric overlaps and linkage to confusion structure are deferred to Section~\ref{sec:discussion}.
