BUILD
-----

From ``report/``:

  latexmk -pdf main.tex

``graphicspath`` searches ``./img/`` first for manuscript figures~(grand-average traces, CSP plots, connectivity topomap, t-SNE) then ``../`` as fallback.

Output: ``main.pdf``.
