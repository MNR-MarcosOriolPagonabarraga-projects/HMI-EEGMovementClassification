BUILD
-----

From ``report/``:

  latexmk -pdf main.tex

``graphicspath`` searches ``./img/`` first (grand averages, CSP diagnostics, connectivity topomaps, t-SNE) then ``../`` (e.g. confusion matrices under ``models/``).

Output: ``main.pdf``.
